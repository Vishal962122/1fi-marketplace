import { Screen } from '@/components/ui/Screen';
import { StateView } from '@/components/ui/StateView';

/** Home — stub for this assignment (focus is the Shop tab). */
export default function HomeScreen() {
  return (
    <Screen>
      <StateView emoji="🏠" title="Home" message="Not part of this assignment." />
    </Screen>
  );
}
