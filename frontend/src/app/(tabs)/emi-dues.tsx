import { Screen } from '@/components/ui/Screen';
import { StateView } from '@/components/ui/StateView';

export default function EmiDuesScreen() {
  return (
    <Screen>
      <StateView emoji="🧾" title="EMI Dues" message="Not part of this assignment." />
    </Screen>
  );
}
