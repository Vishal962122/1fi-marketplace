import { Screen } from '@/components/ui/Screen';
import { StateView } from '@/components/ui/StateView';

export default function LimitScreen() {
  return (
    <Screen>
      <StateView emoji="📈" title="Limit" message="Not part of this assignment." />
    </Screen>
  );
}
